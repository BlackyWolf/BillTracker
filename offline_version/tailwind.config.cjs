/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ['./src/**/*.{html,js,svelte,ts}'],
    plugins: [],
    theme: {
        extend: {
            fontFamily: {
                'quicksand': ['Quicksand', 'sans-serif']
            }
        }
    }
}
